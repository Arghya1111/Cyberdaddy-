"""
CyberDaddy - PythonAnywhere Requirements
============================================================
Stripped-down requirements for PythonAnywhere free/paid tier.
Removes: Celery, Redis, S3, Firebase, Prometheus, Sentry (optional),
         all dev/test/code-quality packages.

Install with:
    pip install -r requirements-pythonanywhere.txt
"""

# ── Core Django ────────────────────────────────────────────
Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0

# ── Authentication ─────────────────────────────────────────
djangorestframework-simplejwt==5.3.1

# ── Database ───────────────────────────────────────────────
# SQLite is built into Python — no driver needed!
# Uncomment below if/when upgrading to PostgreSQL on PythonAnywhere:
# psycopg2-binary==2.9.10

# ── Static Files ───────────────────────────────────────────
whitenoise==6.8.2

# ── Utilities ──────────────────────────────────────────────
Pillow==11.0.0
python-decouple==3.8
django-filter==24.3
drf-spectacular==0.27.2

# ── Security ───────────────────────────────────────────────
django-ratelimit==4.1.0
cryptography==43.0.3

# ── AI / ML ────────────────────────────────────────────────
# openai package works with Groq's OpenAI-compatible API
openai==1.57.4
httpx==0.28.1
groq==0.13.0

# ── Production Server ──────────────────────────────────────
# PythonAnywhere uses its own WSGI server (no gunicorn needed)
# but gunicorn is good for local testing
gunicorn==23.0.0

# ── Optional: Sentry error monitoring ──────────────────────
# Uncomment if you have a Sentry DSN:
# sentry-sdk[django]==2.19.2
